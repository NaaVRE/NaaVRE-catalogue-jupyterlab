import React from 'react';
import { Container } from '@mui/material';

export function Catalogue() {
  return (
    <Container
      sx={{
        width: '100vw',
        height: '100vh',
        padding: '1rem'
      }}
    ></Container>
  );
}
